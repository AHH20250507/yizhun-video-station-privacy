# 壹准视频站 · 公开隐私版

这是无需业务后端的静态版本。访问者使用自己的图片、语言和视频 API。

## 数据与隐私

- 只有 API 地址、API Key 和所选模型会保存在当前网站的浏览器 `localStorage` 中。
- 创作会话、生成历史、任务、上传素材、画布、长剧本和生成结果仅存在于当前页面内存。
- 刷新页面、关闭标签页或重新打开网站后，上述临时内容会消失。
- “API 设置”内提供“清除本机 API 配置”按钮。
- 不要在公共电脑保存 API Key；浏览器本地存储不是硬件保险箱。
- 浏览器插件、恶意脚本或网站出现 XSS 漏洞时，仍可能读取浏览器内保存的 Key。

## 本地启动

- Windows：双击 `启动独立版.bat`
- macOS：双击 `启动独立版.command`
- 浏览器地址：<http://127.0.0.1:8795/>

不要直接双击 `index.html`，否则浏览器本地文件安全限制可能影响 API 请求。

## 第一次使用

1. 点击右上角“API 设置”。
2. 分别为图片、语言和视频模块填写 API 地址与自己的 Key。
3. 点击“拉取模型列表”，选择该 Key 负责的模型。
4. 点击“保存配置”。

供应商必须允许浏览器跨域请求（CORS）。如果供应商不允许浏览器直连，需要另行使用服务器代理方案。

## 公开部署

这是纯静态站，可部署到 GitHub Pages、Cloudflare Pages、Netlify、Vercel 等静态托管平台。发布前运行：

```bash
node --check app.js
node --check standalone-client.js
node --test tests/privacy-mode.test.mjs
```

并确认仓库中没有真实密钥：

```bash
rg -n "sk-[A-Za-z0-9_-]{12,}" .
```
